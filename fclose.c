#include<stdio.h>
int main(){
    /*
    FILE *fp;

    fp=fopen("fclosefile.txt", "w");
    fprintf(fp, "%s", "this is an experiment on fopen and fclose\n");//fprintf(filepoiter, "%.." then massage
    fprintf(fp, "%s", "am understanding better !!!!");
    fclose(fp);
    */

    // C program to illustrate fgetc() function
